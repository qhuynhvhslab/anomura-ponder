type Equipment @entity {
  id: ID!
  equipmentId: Int!
  equipmentName: String!
  equipmentType: Int!
  image: String!
  equippedToAnomura: Anomura
}

type Anomura @entity {
  id: ID!
}


enum EquipmentType {
  NORMAL, RARE
}

